# WebSocket和语音识别问题修复计划

## 问题分析

1. **WebSocket连接错误**：
   - 错误信息："WebSocket error: WebSocket is not connected. Need to call 'accept' first."
   - 发生时间：2026-01-16 21:24:47，在二次对话期间
   - 原因：WebSocket连接已关闭，但仍在active_connections列表中，导致发送消息时失败
   - 生命周期管理问题：连接状态检测不及时，异常情况下状态同步失败

2. **语音识别问题**：
   - 问题：非语音音频（背景噪音和静默）被错误处理为有效语音
   - 影响：识别速度显著降低，准确性下降
   - 原因：VAD（语音活动检测）功能未被正确使用，所有音频都被发送进行识别

## 修复方案

### 1. 修复WebSocket连接错误

**修改点**：`/Users/gyc/Desktop/GYC_coding/zyantine/zyantine_rtc/web_server.py`

**修复内容**：
- 增强`send_message`方法的WebSocket状态检查
- 优化WebSocket连接生命周期管理
- 改进异常处理和状态同步
- 添加更详细的日志记录

### 2. 修复语音识别问题

**修改点**：
- `/Users/gyc/Desktop/GYC_coding/zyantine/zyantine_rtc/stt.py`：使用VADProcessor过滤非语音
- `/Users/gyc/Desktop/GYC_coding/zyantine/zyantine_rtc/web_server.py`：优化音频处理管道

**修复内容**：
- 在`transcribe_with_vad`方法中实现VAD功能
- 过滤掉背景噪音和静默段
- 仅处理实际语音内容
- 优化音频处理流程，提高识别速度和准确性

## 具体修复步骤

### 1. 增强WebSocket连接状态管理

**修改`send_message`方法**：
- 添加更详细的WebSocket状态检查
- 改进异常处理逻辑
- 确保状态同步

**修改`websocket_endpoint`函数**：
- 优化连接异常处理
- 确保正确处理连接关闭

### 2. 实现VAD功能

**修改`transcribe_with_vad`方法**：
- 使用VADProcessor处理音频
- 过滤非语音段
- 仅发送语音内容进行识别

**优化音频处理管道**：
- 在web_server.py中添加VAD处理
- 减少不必要的音频传输

## 修复代码示例

### WebSocket修复

```python
async def send_message(self, message: dict, websocket: WebSocket) -> bool:
    """
    发送消息到指定WebSocket连接
    
    Args:
        message: 要发送的消息
        websocket: WebSocket连接对象
        
    Returns:
        bool: 发送结果，True表示发送成功，False表示发送失败
    """
    try:
        # 检查连接是否仍然活跃
        if websocket not in self.active_connections:
            logger.debug(f"WebSocket not in active_connections, cannot send message")
            return False
        
        # 尝试发送消息
        await websocket.send_json(message)
        logger.debug(f"Message sent successfully: {message.get('type')}")
        return True
    except Exception as e:
        logger.error(f"Error sending message: {e}")
        logger.error(f"Message type: {message.get('type')}")
        logger.error(f"WebSocket in active_connections: {websocket in self.active_connections}")
        
        # 从活跃连接中移除无效连接
        if websocket in self.active_connections:
            logger.info(f"Removing inactive WebSocket from active_connections")
            self.disconnect(websocket)
        return False
```

### VAD修复

```python
async def transcribe_with_vad(
    self,
    audio_chunks: list[np.ndarray],
    sample_rate: int = 16000,
    language: str = "zh"
) -> str:
    try:
        if not audio_chunks:
            logger.warning("No audio chunks provided for transcription")
            return ""
        
        # 使用VAD过滤非语音段
        vad_processor = VADProcessor(sample_rate=sample_rate)
        speech_chunks = []
        
        for chunk in audio_chunks:
            is_speech_end, complete_speech = vad_processor.process_chunk(chunk)
            if complete_speech:
                speech_chunks.extend(complete_speech)
        
        if not speech_chunks:
            logger.info("No speech detected in audio chunks")
            return ""
        
        # 合并语音段并进行识别
        combined_audio = np.concatenate(speech_chunks)
        logger.info(f"Transcribing speech: {len(combined_audio)} samples, sample rate: {sample_rate}Hz")
        
        result = await self.transcribe(combined_audio, sample_rate, language)
        logger.info(f"Transcription result: {result[:50]}..." if result else "No transcription result")
        return result
    except Exception as e:
        logger.error(f"Error in transcribe_with_vad: {e}")
        return ""
```

## 测试计划

1. **WebSocket稳定性测试**：
   - 测试多次对话场景
   - 测试网络中断恢复
   - 测试客户端异常关闭
   - 验证没有"WebSocket is not connected"错误

2. **语音识别性能测试**：
   - 测试带背景噪音的音频
   - 测试静默段处理
   - 测量识别速度提升
   - 验证识别准确性

3. **回归测试**：
   - 验证现有功能正常工作
   - 验证WebSocket连接建立正常
   - 验证语音合成正常

## 预期效果

1. **WebSocket错误修复**：
   - 不再出现"WebSocket is not connected. Need to call 'accept' first."错误
   - WebSocket连接状态管理更加健壮
   - 异常情况下能正确处理

2. **语音识别优化**：
   - 非语音音频被正确过滤
   - 识别速度显著提升
   - 识别准确性提高
   - 系统资源使用更高效