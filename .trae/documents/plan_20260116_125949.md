# WebSocket连接管理修复计划

## 问题分析

1. **WebSocket连接生命周期管理问题**：
   - 当WebSocket连接关闭后，系统仍然尝试调用`send_message`函数发送消息
   - 错误发生在`send_message`函数中调用`websocket.send_json(message)`时
   - 这是因为在连接关闭后，相关的异步任务可能仍在执行，试图向已关闭的连接发送消息

2. **连接状态检查缺失**：
   - `send_message`函数没有检查WebSocket连接的状态
   - 没有验证WebSocket连接是否仍然活跃
   - 没有检查WebSocket实例是否在`active_connections`中

3. **音频块接收日志问题**：
   - 系统持续接收音频块
   - 这可能是因为前端在连接关闭后仍在发送音频数据
   - 或者是因为会话管理有问题，导致音频块被重复处理

4. **异常处理不完整**：
   - `send_message`函数没有适当的异常处理
   - 当发送消息失败时，没有清理相关资源

## 修复方案

### 1. 修改`send_message`函数

**修改点**：`/Users/gyc/Desktop/GYC_coding/zyantine/zyantine_rtc/web_server.py` 第79-80行

**修复内容**：
- 添加WebSocket连接状态检查
- 验证WebSocket是否在`active_connections`中
- 添加适当的异常处理
- 返回发送结果状态

### 2. 修改`broadcast`函数

**修改点**：`/Users/gyc/Desktop/GYC_coding/zyantine/zyantine_rtc/web_server.py` 第82-84行

**修复内容**：
- 同样添加连接状态检查
- 处理发送失败的情况
- 遍历副本以避免并发修改问题

### 3. 优化异常处理

**修改点**：所有调用`send_message`的地方

**修复内容**：
- 在所有调用`send_message`的地方添加异常处理
- 确保在连接关闭后不会继续发送消息

### 4. 增强会话管理

**修改点**：`handle_audio_data`函数和会话管理相关代码

**修复内容**：
- 确保在会话结束时清理所有相关资源
- 确保音频块不会被重复处理
- 添加会话存在性检查

### 5. 添加连接状态检查

**修改点**：`process_speech_and_respond_stream`函数

**修复内容**：
- 在发送消息前验证连接状态
- 确保只向活跃连接发送消息

## 修复代码示例

### 修改`send_message`函数

```python
async def send_message(self, message: dict, websocket: WebSocket) -> bool:
    """发送消息到指定WebSocket连接，返回发送结果"""
    try:
        # 检查连接是否仍然活跃
        if websocket not in self.active_connections:
            logger.debug(f"WebSocket not in active connections, cannot send message")
            return False
        
        await websocket.send_json(message)
        return True
    except Exception as e:
        logger.error(f"Error sending message: {e}")
        # 从活跃连接中移除
        if websocket in self.active_connections:
            self.disconnect(websocket)
        return False
```

### 修改`broadcast`函数

```python
async def broadcast(self, message: dict):
    """向所有活跃连接广播消息"""
    # 创建活跃连接的副本，避免并发修改问题
    active_websockets = list(self.active_connections.keys())
    for websocket in active_websockets:
        await self.send_message(message, websocket)
```

### 修改`handle_audio_data`函数

```python
@async_error_handler
async def handle_audio_data(websocket: WebSocket, data: dict, session_id: str):
    try:
        # 检查连接是否仍然活跃
        if websocket not in manager.active_connections:
            logger.debug(f"WebSocket not active, ignoring audio data")
            return
        
        audio_base64 = data.get("audio_data", "")
        if audio_base64:
            audio_bytes = base64.b64decode(audio_base64)
            audio_array = np.frombuffer(audio_bytes, dtype=np.int16)
            
            session = session_manager.get_session(session_id)
            if session:
                session["recorded_audio_chunks"].append(audio_array)
                total_chunks = len(session["recorded_audio_chunks"])
                total_size = sum(len(chunk) for chunk in session["recorded_audio_chunks"])
                logger.info(f"Received audio chunk, session_id: {session_id}, size: {len(audio_array)}, total chunks: {total_chunks}, total size: {total_size} samples")
    except Exception as e:
        logger.error(f"Audio data handling error: {e}")
        import traceback
        traceback.print_exc()
```

### 修改`process_speech_and_respond_stream`函数

```python
# 在所有send_message调用前添加连接状态检查
if websocket not in manager.active_connections:
    logger.debug(f"WebSocket not active, cannot send message")
    return
await manager.send_message({...}, websocket)
```

## 预期效果

1. 不再出现 "Cannot call 'send' once a close message has been sent" 错误
2. 音频块接收日志输出正常，不会在异常情况下持续重复输出
3. WebSocket连接管理逻辑健壮，能正确处理连接关闭和异常断开场景
4. 代码中添加必要的注释说明连接状态检查和异常处理的实现细节
5. 资源管理更完善，避免资源泄漏

## 实施步骤

1. 修改`ConnectionManager`类的`send_message`和`broadcast`方法
2. 修改所有调用`send_message`的地方，添加连接状态检查
3. 优化`handle_audio_data`函数，添加连接状态检查
4. 增强会话管理，确保资源正确清理
5. 添加必要的日志和注释
6. 测试修复效果