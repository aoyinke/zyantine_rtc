## 任务分析

用户要求将当前使用的 Live2D 模型替换为 `/Users/gyc/Desktop/GYC_coding/zyantine/zyantine_rtc/hiyori_pro_zh/` 目录中的模型。

**当前状态**：
- 前端使用的模型是 `kei_vowels_pro`，位于 `vue3-frontend/public/live2d-models/kei_vowels_pro/` 目录
- 新模型 `hiyori_pro_zh` 位于项目根目录的 `hiyori_pro_zh/runtime/` 目录

**需要完成的任务**：
1. 将 `hiyori_pro_zh/runtime/` 目录复制到前端可访问的 `public/live2d-models/` 目录
2. 修改 `Live2DCharacter.vue` 组件，更新模型路径为新模型
3. 测试新模型是否能够正常显示

## 解决方案

### 步骤 1：复制模型文件

**操作**：
- 在 `vue3-frontend/public/live2d-models/` 目录中创建 `hiyori_pro_zh` 子目录
- 将 `hiyori_pro_zh/runtime/` 目录中的所有文件复制到新创建的目录中

**目标路径**：
- `vue3-frontend/public/live2d-models/hiyori_pro_zh/`

### 步骤 2：修改 Live2DCharacter.vue 组件

**操作**：
- 编辑 `Live2DCharacter.vue` 文件
- 将 `modelPath` 从 `/live2d-models/kei_vowels_pro/kei_vowels_pro.model3.json` 更新为 `/live2d-models/hiyori_pro_zh/hiyori_pro_t11.model3.json`

**修改内容**：
```javascript
data() {
  return {
    isLoading: true,
    error: null,
    modelPath: '/live2d-models/hiyori_pro_zh/hiyori_pro_t11.model3.json'
  };
},
```

### 步骤 3：测试模型加载

**操作**：
- 刷新前端页面
- 检查浏览器控制台是否有错误
- 确认新模型是否正常显示

## 预期结果

修改完成后，前端应该能够：
1. 成功加载新的 `hiyori_pro_zh` 模型
2. 不再显示之前的 `kei_vowels_pro` 模型
3. 新模型能够正常显示和响应交互
4. 控制台没有错误信息

## 技术优势

- **简单直接**：只需要复制文件和修改一个路径
- **风险低**：不涉及复杂的代码修改
- **可回滚**：如果新模型有问题，可以轻松切换回原模型

## 注意事项

- 确保所有模型文件都正确复制，包括纹理文件和动作文件
- 检查新模型的 `model3.json` 文件，确保其引用的路径与实际文件结构一致
- 如果新模型有音频文件引用，可能需要像之前一样移除这些引用
- 测试模型在不同屏幕尺寸下的显示效果